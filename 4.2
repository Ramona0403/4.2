P4.2 nota 5


#include <iostream>
#include <cstring>

using namespace std;
int main(){
    string citit,rez,(*cuvant);
    while(cin>>citit)
    {
        if(rez=="")
        {
            rez=citit;
        }
        else if(rez>citit)
        {
            rez=citit;
        }
        
    }
    cout<<rez;
    return 0;
}
